# R6 · 原生窗口外观应用规则与兼容矩阵

## 目标

在不降低现有恢复、完整性和安全热键保护的前提下，让用户按应用明确允许或禁止 JARVIS 原生窗口外观，并在设置页看到当前可见窗口的实际兼容判定。

## 开发范围

1. Host 规则引擎
   - 规范化并限制进程名输入，不接受路径、窗口句柄或通配表达式。
   - 支持 `allow` / `deny` 两种应用规则，系统保护名单始终优先。
   - 用户禁用规则即时恢复已接管窗口；移除或允许规则后按当前模式重新评估。

2. 设置持久化
   - 将 `window-appearance.json` 升级为带 `schemaVersion` 的模式与规则配置。
   - 兼容旧版只包含 `mode` 的配置；无效、重复或受保护进程规则在加载时安全忽略。
   - 原子写入配置，并把规则一致性纳入运行诊断。

3. 兼容矩阵
   - 枚举当前用户可见的顶层窗口，按进程聚合窗口数、合格数、已接管数。
   - 区分 `allowed`、`automatic`、`denied`、`protected`、`limited`，并返回稳定原因码。
   - 对矩阵做短时缓存和条目上限，避免窗口事件高频触发昂贵扫描。

4. Bridge 与前端
   - 新增应用规则设置和移除接口，保持 native/mock 能力一致。
   - 设置页加入进程规则编辑器、已有规则清单和兼容矩阵快捷操作。
   - 输入、忙碌、错误、空状态和窄屏布局均可访问。

5. 验证与交付
   - Host 单元测试覆盖规范化、优先级、容量和迁移数据清洗。
   - Frontend 单元测试覆盖状态规范化与 mock 规则行为。
   - 集中执行 lint、格式、前端测试/构建、Host 构建/测试。
   - 浏览器视觉检查后立即关闭预览与测试页面；原生冒烟后恢复 Explorer 和系统任务栏。
   - 审查、显式暂存本轮文件、直接提交并推送 `main`。

## 完成标准

- 受保护进程不可被用户允许规则覆盖。
- 禁用规则不会遗留已修改的 DWM 属性。
- 旧设置可无损迁移，规则写入可读回。
- 设置页可完成添加、切换、移除规则，并能解释当前窗口的判定。
- 全部自动化检查通过，视觉测试环境清理完成，用户 archive 删除保持未暂存。
