# JARVIS V1 R3 开发计划

## Internal Window Manager & Workspace Experience

状态：`IMPLEMENTED · NON-DESTRUCTIVE ACCEPTANCE PASSED`

计划日期：`2026-07-27`

开发基线：`main @ d982a60`

首要调试环境：当前 Windows 11 测试机

后续兼容环境：Windows 10 1809+ Home / Pro

交付方式：直接在 `main` 开发，完成代码审查后提交并推送；不创建功能分支。

保护项：用户当前未提交的
`assets/archive/jarvis-compact-orb-64-offcenter-v1.png`
删除不属于本轮范围，不恢复、不暂存、不提交。

---

## 1. 本轮目标

把现有独立实现的 File Explorer、Terminal Workbench 和 System Inspector
收敛为统一的 JARVIS 内部窗口系统，使它们具备一致、可预测且接近 Windows
桌面习惯的交互：

1. 统一注册、打开、关闭、激活、最小化、最大化和恢复状态。
2. 支持鼠标拖动、八方向缩放、双击标题栏最大化。
3. 保持窗口在当前工作区可见，分辨率变化后自动重新约束。
4. 任务栏准确显示内部窗口的运行、激活和最小化状态。
5. 最小化不卸载窗口，保留 Explorer 浏览上下文与 Terminal 会话。
6. 持久化窗口尺寸、位置和最大化状态，但不持久化敏感内容。
7. 提供键盘操作、可访问名称和减少动画偏好。

---

## 2. 产品与安全边界

- 内部窗口管理器只管理 JARVIS WebView 内的工具窗口，不伪装为 Win32
  顶层窗口，也不拦截其他 Windows 应用窗口。
- 不持久化终端输入、目录选中项、系统详情或检查器数据。
- 视口变化、异常存储值和旧版本数据都必须安全归一化。
- 高频 pointer move 只更新当前元素样式，pointer up 时才提交一次 React
  状态，避免持续整树重渲染。
- 保留 Windows 原生退出与任务栏恢复路径。
- 本轮不开发窗口贴靠分区、虚拟桌面、跨显示器拖动或第三方窗口嵌入。

---

## 3. 开发任务

### R3-00 — 基线、计划与保护门禁

- 记录当前 `main`、远端和工作树状态。
- 建立本计划与完成定义。
- 全程显式排除用户的 archive 资产删除。

### R3-01 — 统一窗口状态模型

- 建立版本化窗口定义和纯 reducer。
- 实现 open、close、activate、minimize、restore、toggle maximize、
  commit bounds、viewport reflow。
- 关闭或最小化当前窗口后，自动激活可见窗口中层级最高者。
- 对位置、尺寸、z-index 和持久化数据进行边界约束。

### R3-02 — 工作区窗口框架

- 建立共享 `ManagedWorkspaceWindow`。
- 支持标题栏拖动、八方向缩放、双击最大化。
- 拖动与缩放采用 DOM 临时预览，结束时单次提交状态。
- 提供活动层发光、最小化隐藏和最大化布局。

### R3-03 — 三个内部工具接入

- File Explorer、Terminal Workbench、System Inspector 使用同一状态源。
- 统一标题栏的最小化、最大化/恢复、关闭按钮。
- 最小化后保持组件挂载与业务状态。
- Explorer 新路径请求不因普通恢复而重复挂载。

### R3-04 — 任务栏同步

- Explorer 和 Terminal 复用已有固定图标。
- Inspector 运行时作为动态任务栏项出现。
- 点击活动窗口最小化；点击非活动或已最小化窗口恢复并激活。
- 未运行、运行中、活动和最小化状态具有可访问文本与准确视觉状态。

### R3-05 — 键盘与可访问性

- `Alt+F4`：关闭活动内部窗口。
- `Alt+F9`：最小化活动内部窗口。
- `Alt+F10`：最大化/恢复活动内部窗口。
- `Ctrl+Alt+ArrowLeft/ArrowRight`：在可见内部窗口间切换。
- 标题栏和调整手柄具备角色、标签与键盘快捷键信息。

### R3-06 — 响应式、性能与持久化

- 窗口约束尊重顶部状态栏和底部任务栏。
- 分辨率变化后保持标题栏可达，并重新约束尺寸。
- `localStorage` 只保存版本、边界与最大化状态。
- 支持 `prefers-reduced-motion`。

### R3-07 — 自动化质量门禁

- 为 reducer、视口约束、激活回退、最大化恢复和持久化归一化补单元测试。
- 通过前端 lint、格式检查、单元测试与生产构建。
- 审查事件监听、pointer capture、计时器和 ResizeObserver 清理。

### R3-08 — 本机非破坏性验收

- 在浏览器验证打开、层级、拖动、缩放、最小化、最大化、任务栏恢复、
  键盘操作和刷新后布局恢复。
- 验证至少 1366×768、1920×1080 和当前高分辨率布局。
- 集中执行一次 Host 构建/启动验收，不反复编译。
- 获取必要截图后关闭测试页面与进程，恢复原生桌面和任务栏。
- 最终确认 JARVIS 测试进程残留为 0。

### R3-09 — 审查与直接发布

- 审查代码缺陷、冗余、可访问性、性能和安全边界。
- 复核 diff 与 secrets，确认 archive 删除未被暂存。
- 直接提交到 `main` 并推送 `origin/main`。
- 等待远端 CI 完成并记录结果。

---

## 4. 完成定义

- 三个内部窗口由同一个窗口管理器驱动。
- 所有窗口可拖动、缩放、最小化、最大化、恢复、关闭和重新激活。
- 任务栏与内部窗口状态双向同步。
- 最小化不会丢失 Explorer/Terminal 当前会话。
- 异常布局和分辨率变化不会让标题栏永久移出可视区。
- 单元测试覆盖核心状态转换；lint、格式、测试和生产构建全部通过。
- 浏览器和本机 Host 验收完成，所有测试窗口与进程已关闭。
- 用户 archive 删除保持未提交状态。
- 审查后直接提交并推送 `main`，远端 CI 通过。

---

## 5. 完成记录

执行结果（2026-07-27）：

- R3-00 至 R3-08 已完成；Explorer、Terminal、Inspector 统一使用同一个
  reducer、窗口框架、视口约束和版本化布局存储。
- 最小化保持组件挂载；浏览器实测 Explorer 搜索状态在任务栏最小化与恢复后
  保持不变，Terminal 会话也不因最小化而销毁。
- 浏览器实测标题栏拖动、右下角缩放、最大化/恢复、层级激活、
  `Alt+F9`、`Alt+F10`、`Ctrl+Alt+ArrowLeft/ArrowRight` 均生效。
- Inspector 作为优先可见的动态任务项；Explorer 与 Terminal 复用固定项，
  运行、活动和最小化状态与窗口状态一致。
- 增加白名单化跨 WebView 通道；独立 Taskbar Surface 实测能够同步内部窗口，
  并能向 Desktop Surface 发送最小化、恢复和关闭命令。
- 1366×768、1920×1080、2560×1440 下均无页面滚动溢出，活动窗口不覆盖
  任务栏；视觉验收据此修正了最大化窗口与任务栏之间原有的 20px 重叠。
- 前端 14 项测试、lint、格式检查、生产构建、`npm audit --omit=dev`
  均通过；Host 13 项测试通过，Debug 构建为 0 警告、0 错误。
- Windows 11 build 26200 安全模式生命周期验收返回 `READY`；Explorer 与原生
  任务栏始终可用，结束后 `Jarvis.Host` 与预览监听进程残留均为 0。
- R3-09 审查未发现待阻断问题；用户的 archive 资产删除保持未暂存并排除，
  本轮按约定直接提交并推送 `main`。
