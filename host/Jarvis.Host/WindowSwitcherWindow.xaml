<Window x:Class="Jarvis.Host.WindowSwitcherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="JARVIS Window Switcher"
        Width="1040"
        Height="360"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Background="{StaticResource NativeShellBackgroundBrush}"
        Opacity="0"
        IsHitTestVisible="False"
        SourceInitialized="OnSourceInitialized"
        Loaded="OnLoaded"
        Closing="OnClosing">
  <Grid>
    <wv2:WebView2 x:Name="WebView"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  DefaultBackgroundColor="#000000" />

    <Border x:Name="LoadingOverlay"
            Background="{StaticResource NativeShellBackgroundBrush}"
            IsHitTestVisible="False">
      <StackPanel HorizontalAlignment="Center"
                  VerticalAlignment="Center">
        <TextBlock Text="WINDOW CHANNEL"
                   Foreground="{StaticResource NativeShellInkBrush}"
                   FontFamily="Bahnschrift"
                   FontSize="13"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center" />
        <TextBlock x:Name="StatusText"
                   Text="INITIALIZING"
                   Margin="0,8,0,0"
                   Foreground="{StaticResource NativeShellMutedBrush}"
                   FontFamily="Consolas"
                   FontSize="9"
                   HorizontalAlignment="Center" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
