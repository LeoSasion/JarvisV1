<Window x:Class="Jarvis.Host.TaskbarFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JARVIS Task Switcher"
        Width="520"
        Height="220"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Background="{StaticResource NativeShellBackgroundBrush}"
        SourceInitialized="OnSourceInitialized"
        Loaded="OnLoaded"
        PreviewKeyDown="OnPreviewKeyDown"
        Closed="OnClosed">
  <Border BorderBrush="{StaticResource NativeShellStructureBrush}"
          BorderThickness="1"
          Background="{StaticResource NativeShellBackgroundBrush}"
          CornerRadius="0"
          SnapsToDevicePixels="True">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="40" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid Grid.Row="0" Margin="14,0,8,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Width="3"
                Height="14"
                Margin="0,0,10,0"
                Background="{StaticResource NativeShellAccentBrush}"
                VerticalAlignment="Center">
          <Border.Effect>
            <DropShadowEffect Color="{StaticResource NativeShellAccentColor}"
                              BlurRadius="5"
                              ShadowDepth="0"
                              Opacity="0.55" />
          </Border.Effect>
        </Border>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock x:Name="TitleText"
                     Foreground="{StaticResource NativeShellInkBrush}"
                     FontFamily="Bahnschrift"
                     FontSize="11"
                     FontWeight="SemiBold" />
          <TextBlock x:Name="MetaText"
                     Margin="10,0,0,0"
                     Foreground="{StaticResource NativeShellMutedBrush}"
                     FontFamily="Consolas"
                     FontSize="9" />
        </StackPanel>
        <Button Grid.Column="2"
                Width="30"
                Height="28"
                Content="×"
                Foreground="{StaticResource NativeShellMutedBrush}"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="Segoe UI"
                FontSize="17"
                ToolTip="Close preview"
                Click="OnCloseFlyoutClick" />
      </Grid>

      <Border Grid.Row="0"
              Height="1"
              Margin="14,39,14,0"
              Background="{StaticResource NativeShellStructureBrush}"
              VerticalAlignment="Top" />

      <WrapPanel x:Name="CardsPanel"
                 Grid.Row="1"
                 Margin="8,7,8,9"
                 KeyboardNavigation.DirectionalNavigation="Cycle"
                 KeyboardNavigation.TabNavigation="Cycle"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Top" />
    </Grid>
  </Border>
</Window>
